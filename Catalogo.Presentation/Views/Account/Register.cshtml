@{
    ViewData["Title"] = "Registro";
}

<div class="cr-page">
    <h1 class="cr-title">Crear Cuenta</h1>

    @if (ViewBag.Error != null)
    {
        <p class="cr-error">@ViewBag.Error</p>
    }

    <form class="cr-form" asp-action="Register" method="post">
        <div class="cr-form-group">
            <label class="cr-form-label">Nombre</label>
            <input class="cr-form-input" name="nombre" />
        </div>
        <div class="cr-form-group">
            <label class="cr-form-label">Email</label>
            <input class="cr-form-input" name="email" type="email" />
        </div>
        <div class="cr-form-group">
            <label class="cr-form-label">Contraseña</label>
            <input class="cr-form-input" name="password" type="password" />
        </div>

        <button type="submit" class="cr-btn-guardar">Registrarme</button>
    </form>

    <div class="cr-volver-wrapper">
        <a class="cr-volver" asp-action="Login">¿Ya tienes cuenta? Inicia sesión</a>
    </div>
</div>